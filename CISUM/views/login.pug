link(rel="stylesheet", href="/stylesheets/login.css")
body
    article(id="login_body")
        form(action="/cisum/join", method="post")
            h2 LOGIN
            input(name="email", placeholder="ID", typw="text")
            input(name="m_password", placeholder="Password", type="password")
            button 로그인
            button(id="register") 회원가입